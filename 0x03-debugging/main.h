#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
in largest number(int a, int b, int c);
void pint remaining_days(int month, int day,int year);
int convert day(int month, int day);

#endif /* MAIN_H */
